<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TestSheet.SheetSuggestionActivity"></activity>
        <activity
            android:name=".TestSheet.SheetMainActivity"
            android:windowSoftInputMode="adjustPan|stateHidden" />
        <activity android:name=".Subscription.SubscriptionMainActivity" />
        <activity android:name=".Login.SigninActivity" />
        <activity android:name=".Memory.MemoryMainActivity" />
        <activity android:name=".Login.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login.LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustPan|stateHidden">

            <!-- 用于设置该活动中由于软键盘弹出导致整个界面向上进行移动的情况 -->
        </activity>
    </application>

</manifest>